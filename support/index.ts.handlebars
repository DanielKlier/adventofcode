// Solution for {{year}}, day {{day}}
import { Logger } from "../../utils/log.ts";
import { assertEquals } from "../../utils/test.ts";

type InputType = unknown;

class Day{{day}}<T = InputType> {
  logger = new Logger(Logger.Level.Error);

  private parse(input: string): T {
    return null as unknown as T;
  }

  private part1(input: T): number {
    return 0;
  }

  private part2(input: T): number {
    return 0;
  }

  run(input: string) {
    this.test();

    const parsedInput = this.parse(input);

    console.log(`Part1: ${this.part1(parsedInput)}`);
    console.log(`Part2: ${this.part2(parsedInput)}`);
  }

  private test() {
    assertEquals(true, false);

    this.logger.info("Tests OK");
  }
}

export default function runDay{{day}}(input: string) {
  new Day{{day}}().run(input);
}
